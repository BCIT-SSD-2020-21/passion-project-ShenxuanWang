import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    images: {
        height: 150,
        width: 150,
        resizeMode: 'contain',
        justifyContent: 'center',
        alignItems: 'center'
    },
});